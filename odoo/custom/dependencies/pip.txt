git+https://github.com/OCA/openupgradelib.git@master
unicodecsv
unidecode

# Pipeline LiDAR HD (ibatix_solar) — présents sur PROD via pip install manuel
# jamais capturé nulle part, d'où l'absence sur la flotte (numpy manquant).
numpy==2.4.4
laspy==2.7.0
lazrs==0.8.1
scipy==1.17.1
scikit-learn==1.8.0
pyproj==3.7.2

# Cachet cryptographique PAdES des PDF signés (ibatix_sign phase 4).
# ⚠ pins OBLIGATOIRES : pyhanko récent tire cryptography>=50, en conflit
# avec pyopenssl 24 (composant Odoo) — même pin que le Dockerfile PROD.
cryptography==42.0.8
pyhanko==0.23.2
pyhanko-certvalidator<0.27

# PDF : ibatix_report_chromium / ibatix_devis_chromium (numérotation, fusion) — présent sur PROD, absent de l'image doodba.
pypdf==6.10.2
